# 当前实现与本地开发

本项目是基于 NES 模拟器的 Mario 控制实验：读取游戏 RAM，生成结构化状态，交给规则或模型选择动作，再由执行器落实按键。纯模型模式原样执行模型选择，默认混合模式允许安全层改写。仓库不包含权重，提供独立的本地 Qwen 推理适配器。

上游 [4esv/jev-mario](https://github.com/4esv/jev-mario) 已另行克隆到同级 `jev-mario-upstream` 目录。对比确认，原有 `play.py`、`branch.py`、`live.py` 仅增加了环境兼容初始化；双模型、教练、恢复路线和网页看护属于扩展实现。

## 模块

| 文件 | 职责 |
| --- | --- |
| `mario_env.py` | 适配 Gym / Gymnasium 环境的 reset / step 接口。锁定环境使用 Gym 0.26、nes-py 8.2.1 和 NumPy 1.26。 |
| `play.py` | RAM → 地形网格 → 状态摘要；动作字典、跳跃测量、规则策略、Jev 直接控制与日志。 |
| `branch.py` | 使用模拟器快照试演动作及后续路径；规则搜索或 Jev 从已测量的结果中选择。 |
| `live.py` | 模拟调用延迟对不停顿控制的影响；用于实时性实验。 |
| `play_local.py` | 本地 OpenAI 兼容接口、Jev 接口、教练计划解析、安全守卫、脱困和失败诊断。 |
| `watch_local.py` | 两个独立模拟器、同步决策 / 异步实验、MJPEG 画面、`/state` 状态、跨局教训和恢复路线。 |
| `mentor_search.py` | 重复失败后的可选攻略检索；不在逐帧控制路径中执行。 |
| `supervise.py` | 看护观战页和可选隧道；现在支持 macOS / Linux 的存活检查及完整子进程回收。 |
| `serve_qwen.py` | 读取缓存或指定目录中的 Qwen 权重，提供 localhost Chat Completions、模型列表和健康检查。 |
| `verify_routes.py` | 不依赖模型服务的重复回放验收，输出逐步证据和带标签的 GIF。 |
| `make_dual_gif.py` | 将已有成功录制合成为双画面演示；历史录像不等于当前代码的重新验收。 |

## 执行与证据边界

原仓库首页三段 GIF 对应 **1-1、2-1、3-1**。`runs/results.jsonl` 中三条同名记录都标记 `bot=branch-jev`、`flag=true`，分别用了 23、29、25 次 Jev 请求，属于历史结果。当前首页已经替换为 2026-09-23 真实模型实测；本轮 Jev 纯模型 0/9、本地 Qwen 0/3 通关，重新运行的 1-1 branch-Jev 也未通关。完整证据见 [README](README.md) 与 [评测汇总](artifacts/model-eval-20260923/summary.json)。

这些录像使用 `branch.py` 的在线分支试演：从当前快照试跑每个候选动作（基本预算 60 帧，等落地后再稳定 8 帧），再测试两步后续动作。当前版本有 11 个候选动作、6×6 个后续组合。将死亡、位移、存活后续数量和最佳路径转为文字，交给 Jev 做结构化 `choice`；恢复同一个快照后，按试演相同的时序执行所选动作。若没有可推进选择，再做三步脱困搜索。`--bot search` 可以用确定性评分代替模型选择。

这个方法的主要收益来自实际模拟器提供的前瞻结果；普通 `play.py` 只给模型当前 RAM 摘要，`live.py` 还引入调用延迟，原仓库记录的成绩明显更低。分支试演计算量也更高：README 记录每关需要约 15–20 分钟墙钟时间，而 GIF 只收录最终执行路径。

原 README 特别注明，2-1 和 3-1 的成功录像来自更早的 `branch.py` 动作集；当前构建的对应结果止于 x=2066 / 2770。所给 GitHub 仓库不是平台登记的 fork，且仅有一个打包提交。上游 [4esv/jev-mario](https://github.com/4esv/jev-mario) 使用相同 README 和 GIF 文件，保留了原始开发历史；追查早期实现应从那里开始，不能把当前主分支视为历史录像的逐帧可复现版本。

默认混合模式中，模型输出先经过教练计划、安全守卫和脱困逻辑。每条教练计划具有来源、编号、执行轨迹和失败计数；两步等待 / 后撤后跳跃的计划可以被实际执行。连续失败会积累场景教训。`--model-only` 关闭这些辅助能力和规则兜底，保留原始模型选择与执行动作，非法回答或服务错误会停止该栏。

默认混合模式下，有恢复路线的关卡连续失败三局后，从下一局开始启用路线；`--verified-route` 可以立即启用。此时模型继续被请求，原始选择保留在遥测中，但最终动作由恢复路线决定。纯模型模式禁止启用恢复路线。`--replay-only` 完全跳过模型、教练和搜索请求，页面明确显示“离线验证路线回放（未调用模型）”。回放完成或失败后停留，不无限重试。

“同步”指每个模拟器等待自己的决策，不意味着两条通道按墙钟时间逐帧锁步。两条通道的推理速度可以不同。

原仓库记录了 1-1 本地 Qwen 与 Jev 的成功结果，以及 1-2 路线辅助成功；其来源与复现限制见 [录像索引](artifacts/current/README.md)。离线回归与本轮真实模型试验分别统计，后续关卡不能由某条路线的成功推断为已完成。

## 本地运行

需要 `uv`。它根据项目约束选择 Python 3.12，并安装锁定依赖：

```bash
uv sync --frozen
uv run python supervise.py --level 1-3 --replay-only
```

打开 <http://127.0.0.1:8123/>。离线模式自动关闭公网隧道，两个通道都显示同一恢复路线，不需要 `.env`。按 Ctrl+C 关闭；看护进程会等待子进程退出、回收锁和监听端口。

重新接入真实模型时，复制 `.env.example` 为 `.env` 并配置自己的服务。默认本地策略接口为 `http://127.0.0.1:11500/v1`，模型名由 `LOCAL_POLICY_MODEL` 指定；官方策略需要 `TYPESAFE_API_KEY`，教练需要单独的服务配置。然后运行：

```bash
uv run python supervise.py --level 1-1 --model dual --model-only --stay-on-level --tunnel none
```

上述纯模型模式遇到请求失败会停止该栏；默认混合模式可能退回规则策略，不能据此声称模型调用成功。viewer 的 `calls` 包含失败尝试；`play_local.py` 的 `api_calls` 统计有效返回，两个字段口径不同。结果中的 `model_decision_sources` 单独记录决策来源，失败时 `model_choice=null`。本地 Qwen 服务的启动和双栏完整命令见 [README](README.md#双栏实时观战)。

## 验证

```bash
uv run python -m unittest discover -s tests -v
uv run python verify_routes.py --repeats 2 --gif
```

自动测试在真实模拟器上检查恢复路线到达旗杆、没有 HTTP 请求、帧数逐步连续以及路线用尽不会切换到其他控制器；看护脚本测试覆盖进程检测、锁、重启、终止和参数传递。Windows 进程检查采用模拟测试，仍需 Windows 原机验证。

验收脚本会遍历已登记路线，各运行两次，写入 `output/verified-routes/acceptance.json`。每次都必须 `flag=true`、`route_complete=true`、`calls=0`，并且同一关卡的距离与帧数一致；不满足时返回非零退出码。

新观战页的 `frames` / `/state.frame` 统计真实 `env.step()` 次数；`decision_clock` 保留原来的调度时钟。历史 1-2 的 2352 是含对齐补位的调度计数，实际是 2300 模拟器帧（38.33 秒）。历史 GIF 和记录仍保留原样，新验收 GIF 按真实模拟器 60 fps 抽样为 10 fps。

## 前期执行器回归结果

| 关卡 | 验收方法 | 结果 |
| --- | --- | --- |
| 1-2 | 原有 23 步路线，独立重复回放两次 | `flag=true`，x=3161，真实 2300 帧，0 次模型请求 |
| 1-3 | 原版 `branch.py --bot search` 搜索，再接入观战页独立重复两次 | `flag=true`，x=2425，真实 2430 帧，0 次模型请求 |

1-3 的两段三步 `escape` 展开后为 26 个路线步骤，保留原来的按键、助跑、落地和后续动作时序。普通单步贪心探测会卡在 x≈631；原版前瞻搜索能够通过这段长坑并完成关卡。

这些结果验证搜索和路线执行器，不计入模型成绩。本轮已经测试 1-3 纯模型执行但未通关，1-4 及后续连续推进仍未验证。
