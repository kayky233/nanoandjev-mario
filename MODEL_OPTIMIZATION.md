# 模型优化实测：2026-09-23

本轮只调整模型观测、提示词和本地推理，不修改观战传输或动作执行器。新增 **33 局、475 次真实模型决策**，包括未采用的候选；所有回答原样执行，未开启 guard、自动脱困、教练、规则兜底或路线接管。**没有一局通关，不能宣称整体能力已解决。**

## 已确认的性能收益

在本机 Apple M5 / 16 GiB 上暂停自动对局，用相同的 18 个历史请求串行比较。均为已有 Qwen2.5-1.5B-Instruct 权重、同一 revision、FP16、贪心解码，没有量化或下载新权重。第一请求单列，其余 17 个请求统计热请求延迟。

| 后端 | 首请求 | 热请求 p50 | 热请求 p95 | 与旧回答一致 |
| --- | ---: | ---: | ---: | ---: |
| 原 PyTorch / MPS | 5.934 s | 1.344 s | 1.853 s | 18/18 |
| MLX，无前缀缓存 | 8.660 s | 1.505 s | 4.894 s | 18/18 |
| MLX，前缀缓存 | 2.233 s | 1.103 s | 4.475 s | 18/18 |
| MLX，前缀缓存 + 进程固定 wired limit | 3.401 s | **0.953 s** | **1.142 s** | 18/18 |

最终配置的 p50 下降约 **29%**，p95 下降约 **38%**。只换 MLX 没有提速，必须保留这个负结果。实验按顺序运行，受编译、机器负载和内存状态影响；这是本机小样本结果，不是硬件排行榜。原基线报告的约 0.8 秒采集于更早的运行时段，不能混用为本轮受控对照。

前缀缓存只复用从开头起完全一致的输入 token；每次生成后裁掉回答的 KV，下一次观察有变化就裁到分歧处重新计算。它不缓存动作答案，不替模型选动作。固定 wired limit 只作用于当前 MLX 进程，未修改系统全局设置。适配器使用 [MLX LM](https://github.com/ml-explore/mlx-lm)。

另以三种新提示的 23 个真实请求检查缓存开关，输出 23/23 相同，覆盖不同动作、不同固定前缀、状态切换和重复请求。这检查推理一致性，不检查动作正确性；46 项回归测试通过。

## 决策质量：局部改善，仍有退步

修复的输入缺陷包括：

- 坑和墙原先只扫描前 8 格，模型会漏看第 9 格的断层；现在扫描有效视野，并明确未知边界、坑宽是否仅为下界。
- 前方墙壁曾被误当作头顶天花板，导致跳跃落点估计失真；现在区分头顶障碍与前方墙壁，保留高平台的完整可见地形。
- 同一提示曾同时要求“禁止完整跳跃”和“只能完整跳跃”；现在将落点和敌人运动预测标为估计，保留可比较的风险指导。
- 删除唯一偏向低跳的示例；完整动作定义与规则放在固定 system 前缀，动态状态与历史放在后面。

共比较三个候选：v1 修观测并采用紧凑结构；guided 在 v1 上增加一段落点敌人风险指导；prose 保留 guided 规则但改回自然语言摘要和网格。最终采用 guided 作为下一轮实验配置，**没有按关卡切换提示或挑选某一条通关路线**。

| 模型 / 配置 | 1-1 最远 x | 1-2 最远 x | 1-3 最远 x | 通关 |
| --- | --- | --- | --- | --- |
| 原 Jev 基线，各 3 局 | 1435 / 679 / 2471 | 198 / 655 / 960 | 284 / 315 / 315 | 0/9 |
| Jev v1，各 3 局 | 315 / 1797 / 1797 | 904 / 1338 / 904 | 303 / 303 / 303 | 0/9 |
| Jev guided，各 3 局 | 1794 / 1794 / 1794 | 902 / 906 / 903 | 303 / 303 / 303 | 0/9 |
| 原 Qwen 基线，各 1 局 | 722 | 656 | 618 | 0/3 |
| Qwen v1，各 1 局 | 709 | 676 | 414 | 0/3 |
| Qwen guided，各 3 局 | 1411 / 1411 / 1411 | 658 / 658 / 658 | 410 / 410 / 410 | 0/9 |
| Qwen prose，各 1 局 | 1130 | 673 | 311 | 0/3 |

Qwen 从原来的 54/54 次低跳，变为会选择多种动作；guided 的 1-1 推进距离可重复地增加，但 1-3 明显退步。Jev 的 guided 中位推进距离在 1-1、1-2 提升，1-3 无改善，原基线的最佳 1-1 距离仍更远。因此只支持“部分场景改善”，不能支持“整体成功率提高”。本轮提示利用已见失败场景迭代，属于探索实验，未做独立留出关卡验收。`jev-latest` 也不是可固定的服务端权重版本。

保留 guided 的原因是它在当前观战关卡 1-1 上更稳定，且修正了已证实的观测错误；部署到其他关卡或业务前必须继续验收。三次相同结果只说明本配置轨迹可重复，不代表有三个独立多样样本。

## 复现与证据

[逐局结果、动作归因和源码摘要](artifacts/model-optimization-20260923/summary.json)记录了全部新增实验；[原始证据压缩包](artifacts/model-optimization-20260923/game-evidence.tar.gz)包含 HTTP 请求/响应、逐决策日志、每局结果以及 baseline/v1/guided/prose 四份控制器快照，不含认证头或凭据。GIF 留在本机 `output/model-opt-20260923/`，压缩包只收录数据和源码，不收录录像。性能及缓存一致性数据见同目录 `inference-evidence.json`。

```bash
# 只加载本机已有权重；依赖与游戏虚拟环境分离。
uv run --script serve_qwen_mlx.py --port 11504
```

另一个终端：

```bash
export LOCAL_POLICY_BASE_URL=http://127.0.0.1:11504/v1
export LOCAL_POLICY_MODEL=Qwen2.5-1.5B-Instruct
export LOCAL_POLICY_MODE=chat
export NO_PROXY=127.0.0.1,localhost
uv run python evaluate_models.py --bot local --attempts 3 --output output/new-local-eval
# 注入自己的 TYPESAFE_API_KEY 后评测 Jev：
uv run python evaluate_models.py --bot jev --attempts 3 --output output/new-jev-eval
```

`--output` 必须为空，避免混入旧结果；`--controller` 可指定解压后的控制器快照做对照。`--no-prefix-cache` 可关闭 MLX 前缀缓存，其他推理设置不变。该适配器仅支持 Apple Silicon GPU、localhost、非流式 chat、temperature=0；不是行内通用生产推理网关。行内 Linux / GPU 部署继续使用报告中的推理服务和隔离方案。

## 接下来应投入哪里

优先处理 1-3 等失败场景中的空间关系与动作后果判断。重复同一局不会让现有模型自动学习；继续无限重试不能代替训练或新的上下文信息。用失败前的真实状态构造动作选择集，比较更强模型与任务微调，再以未见状态、完整新对局和动作来源记录验收。训练样本可以包含模拟器的反事实后果，但评测时仍需明确模型是否使用了试演工具，不把路线回放计作模型能力。

对 agent 业务，当前最直接的收益是固定工具定义的前缀缓存、准确且无矛盾的状态、明确的动作结果与可审计的评测。浏览器任务应测端到端成功率和每步等待；代码检视应测有效问题发现率、误报和每个有效发现的耗时。不能用 Mario 的延迟下降直接推算业务 ROI。Laya 的 typed-choice 方向适合后续对照，但其接口形式本身不能让未经任务训练的小模型具备更强决策能力。
